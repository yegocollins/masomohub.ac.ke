const chatModel = require('../models/chat_model');
const moderationRules = require('../models/moderation_model');
const aiService = require('../utils/gemini_service');

class Moderation{
    /**
     * Implement flag submission here
     * 
     * When a submission is flagged, add it to flagged submission in the database.
     */
}

module.export = Moderation;